import 'package:flutter/material.dart';

class EsewaPage extends StatelessWidget {
  const EsewaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Esewa Payment Test'),
      ),
      body: Text('Esewa Page'),
    );
  }
}